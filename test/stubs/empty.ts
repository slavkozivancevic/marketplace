// No-op stub aliased in vitest.config.ts for `server-only` / `client-only`.
// Those packages throw when resolved outside their intended bundler context;
// under the node test runner they're irrelevant, so they map here.
export {};
